language: node_js
node_js:
  - "8"
cache:
  directories:
    - "node_modules"
script:
  npm run build

before_deploy:
  # Set up git user name and tag this commit
  - git config --local user.name "st2135"
  - git config --local user.email "loed@mail.ru"
  - git tag "$(date +'%Y%m%d%H%M%S')-$(git log --format=%h -1)"

deploy:
  provider: releases
  api_key: "f613f5c2f9a58cba070c693e7c962e9c7e11344e"
  file_glob: true
  file: dist/*
  skip_cleanup: true
  on:
    tags: false
