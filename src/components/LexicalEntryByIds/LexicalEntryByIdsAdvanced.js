import React, { PureComponent } from 'react';

class LexicalEntryByIdsAdvanced extends PureComponent {
  constructor() {
    super();

    // const { perspectiveId, entriesIds } = props;

    this.state = {
      entriesIds: [],
    };
  }

  render() {
    // const {
    //   className, perspectiveId, entriesIds, defaultMode, entitiesMode, actions,
    // } = this.props;
    return (
      <div>
        Продвинутый компонент
      </div>
    );
  }
}

export default LexicalEntryByIdsAdvanced;
