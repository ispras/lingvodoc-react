const { defer } = require('lodash');

import React from 'react';
import {Segment} from 'semantic-ui-react'
function test(props) {
  console.log(props);
  return (
  <Segment>
      werw
  </Segment>
  );
}

export default test;
